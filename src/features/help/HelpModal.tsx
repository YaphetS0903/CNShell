import { Command, FileUp, Search, ShieldCheck, TerminalSquare } from "lucide-react";
import { Modal } from "../../components/Modal";
import { useAppStore } from "../../store/app-store";

export default function HelpModal(){const{helpOpen,setHelpOpen,settings,saveSettings}=useAppStore();if(!helpOpen)return null;return <Modal title="CNshell 使用帮助" onClose={()=>setHelpOpen(false)} wide><div className="help-content"><div className="help-hero"><TerminalSquare size={34}/><div><h3>连接、操作、传输，一处完成</h3><p>CNshell 使用 macOS Keychain 保护凭据，并严格校验每台服务器的主机指纹。</p></div></div><div className="help-grid"><Help icon={Command} title="终端快捷键" lines={["⌘T 新终端","⌘W 关闭会话","⌘F 搜索输出","⌘K 清屏","⌘1…9 切换标签"]}/><Help icon={FileUp} title="远程文件" lines={["双击文件夹进入","双击文本打开编辑器","上传下载进入后台队列","保存时检查远端冲突"]}/><Help icon={Search} title="诊断连接" lines={["连接测试区分 TCP、指纹和认证错误","首次连接请在其他可信渠道核对指纹","指纹变化时 CNshell 会阻止连接"]}/><Help icon={ShieldCheck} title="隐私与安全" lines={["密码不写入数据库和日志","遥测默认关闭","下载先写入 .cnshell-part","RDP 使用外部 FreeRDP Helper"]}/></div><footer className="help-footer"><label><input type="checkbox" checked={!settings.showWelcomeHelp} onChange={(event)=>void saveSettings({...settings,showWelcomeHelp:!event.target.checked})}/>不再自动显示</label><button className="button primary" onClick={()=>setHelpOpen(false)}>知道了</button></footer></div></Modal>}
function Help({icon:Icon,title,lines}:{icon:typeof Command;title:string;lines:string[]}){return <section><h3><Icon size={17}/>{title}</h3>{lines.map((line)=><p key={line}>{line}</p>)}</section>}
